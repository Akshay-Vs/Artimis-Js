export type StyleType = {
  theme?: {
    dark: {
      primary: string;
      secondary: string;
      accent: string;
    };
    light: {
      primary: string;
      secondary: string;
      accent: string;
    };
  };
}